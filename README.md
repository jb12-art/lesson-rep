# lesson-rep

Learning about Git and NPM

## npm install

```bash
npm install
```

```bash
npm run dev
```

## Environment Variables

To run this project, you will need to add the following environment variables to your `.env` file.

`API_URL` - The base URL for the backend API (e.g., `https://api.yourapp.com`)
`DB_HOST` - The database server address
`DB_USER` - The database username
`DB_PASS` - The database password

### Example .env Block

Create a file named `.env` in the root directory of the project and add the following contents, replacing the placeholder values with your actual credentials:

## Available Scripts

In the project directory, you can run the following commands:

Command | Description

```bash
npm start       |   Runs the app in development mode. Open http://localhost:3000 to view it in the browser.
```

```bash
npm run build   |   Builds the app for production to the build folder.
```

```bash
npm test        |   Launches the test runner in interactive watch mode.
```

```bash
npm run lint    |   Runs the linter across all project files to check for code style issues.
```

## Technologies used

HTML, JavaScript, eslint, prettier

## Author

jb12-art
